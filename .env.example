VITE_API_BASE=http://localhost:8000
# Optional: Where Vite proxy forwards /api during `npm run dev`
VITE_DEV_PROXY_TARGET=http://localhost:8000
# Optional: Force direct API in dev (bypass proxy). Default false.
VITE_DIRECT_API=false
